<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="#121212"> <ImageView
    android:id="@+id/imgFoto"
    android:layout_width="match_parent"
    android:layout_height="0dp"
    android:layout_weight="1"
    android:background="#333333"
    android:contentDescription="Vista previa de la foto"
    android:layout_marginBottom="20dp"
    android:scaleType="fitCenter"/>

    <TextView
        android:id="@+id/txtEstado"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Hola, soy Kivi"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textAlignment="center"
        android:textColor="#FFEB3B"
        android:layout_marginBottom="30dp"/>

    <Button
        android:id="@+id/btnEscuchar"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:text="MICRÓFONO 🎤"
        android:textSize="24sp"
        android:textStyle="bold"
        android:backgroundTint="#2196F3"
        android:contentDescription="Presiona para hablar con Kivi"
        android:layout_marginBottom="16dp" />

    <Button
        android:id="@+id/btnCamara"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:text="CÁMARA 📷"
        android:textSize="20sp"
        android:textStyle="bold"
        android:backgroundTint="#4CAF50"
        android:contentDescription="Presiona para tomar una foto" />

</LinearLayout>